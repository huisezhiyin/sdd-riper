interface:
  display_name: "New Chat Ready"
  short_description: "精简交接、Git 快照保护并安全续开新对话。"
  default_prompt: "Use $new-chat-ready to compact the handoff and project knowledge, protect dirty Git work with a verified local snapshot commit, then create the next Codex conversation directly when supported; treat remote push as optional and use a paste-ready prompt only as fallback."
policy:
  allow_implicit_invocation: true
